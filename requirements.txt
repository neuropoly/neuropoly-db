# ElasticSearch Python client (9.x compatible with ES 9.x and 10.x)
elasticsearch>=9.0,<10

# Neuroimaging metadata
pybids>=0.16
pydicom>=2.4

# Embeddings (torch installed separately as CPU-only due to SM 6.1 Maxwell limitation)
sentence-transformers>=2.2
transformers>=4.35     # SPLADE sparse encoder (PoC 2, Notebook 07)
matplotlib             # benchmark plots (Notebook 07)

# Jupyter & data
jupyter
ipywidgets
pandas
tqdm

# NIfTI headers (optional, for advanced exploration)
nibabel
